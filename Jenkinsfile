pipeline {
  agent any
  stages {
    stage('') {
      steps {
        dockerNode(credentialsId: 'dockerhub-credentials', dockerHost: 'jvelizvid / kubernetes-demo', image: 'jvelizvid / kubernetes-demo')
      }
    }

  }
}